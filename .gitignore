# Personal Claude project context - notes for AI sessions, not repo docs.
CLAUDE.md

.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
site/



# The scripts that originally generated the notebooks. The .ipynb files are
# now the source of truth and are edited directly, so these are kept local
# only - running one would overwrite the notebook it built.
build_notebook.py

# Staging area for files removed from the project. Delete when you are happy.
_to_delete/

#literature folder where knoweldge of techcniques is kept
literature/
# Private build briefs - kept in the Claude project context, not the repo.
prompts/
